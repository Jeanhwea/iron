spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://minix.jeanhwea.io:3306/database?useUnicode=true&characterEncoding=utf-8&useSSL=false
    platform: mysql
    driver-class-name: com.mysql.jdbc.Driver
  output:
    ansi:
      enabled: always


server:
  port: 9090
